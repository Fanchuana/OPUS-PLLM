from setuptools import setup, find_packages

setup(
    name='multi_modality_model',
    version='0.1',
    packages=find_packages(),
    description='A custom package for cstp project',
    install_requires=[

    ],
)